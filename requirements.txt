discord.py>=2.3.0
anthropic>=0.42.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiosqlite>=0.19.0
structlog>=24.1.0
typer>=0.9.0

# Azure (optional — for DB persistence)
azure-storage-blob>=12.19.0

# Voice (TTS via Azure OpenAI — sent as MP3 attachment)
openai>=1.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Linting & Formatting
ruff>=0.11.0

# Security
pip-audit>=2.7.0
bandit>=1.8.0
